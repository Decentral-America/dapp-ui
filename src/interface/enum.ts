// TODO use one login type
export enum ELoginType {
    CUBENSIS = 'cubensis',
    EXCHANGE = 'exchange',
    METAMASK = 'metamask',
}

// TODO use one login type
export enum LoginType {
    SEED,
    EMAIL,
    METAMASK,
}
